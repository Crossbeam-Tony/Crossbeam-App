import 'package:flutter/material.dart';

class CrewsfeedPage extends StatelessWidget {
  const CrewsfeedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Crewsfeed Page'),
      ),
    );
  }
}
